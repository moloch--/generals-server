name: generals-server-aws

services:
  generals-server:
    image: ${GENERALS_IMAGE:?set GENERALS_IMAGE}
    platform: ${GENERALS_PLATFORM:?set GENERALS_PLATFORM}
    container_name: generals-server
    user: "65532:65532"
    restart: unless-stopped
    stop_grace_period: 15s
    read_only: true
    command:
      - --control-listen=:29900
      - --relay-listen=:27901
      - --health-listen=:8080
      - --admin-listen=:8081
      - --public-web-listen=:8443
      - --public-web-tls-cert=/tls/fullchain.pem
      - --public-web-tls-key=/tls/privkey.pem
      - --public-web-redirect-listen=:8083
      - --public-web-canonical-host=${GENERALS_PUBLIC_WEB_HOST:?set GENERALS_PUBLIC_WEB_HOST}
      - --public-host=${GENERALS_PUBLIC_HOST:?set GENERALS_PUBLIC_HOST}
      - --data-file=/data/profiles.db
      - --tls-cert=/tls/fullchain.pem
      - --tls-key=/tls/privkey.pem
      - --admin-token-file=/run/secrets/admin-token
      - --admin-tls-cert=${GENERALS_ADMIN_TLS_CERT:-}
      - --admin-tls-key=${GENERALS_ADMIN_TLS_KEY:-}
    ports:
      - "29900:29900/tcp"
      - "27901:27901/udp"
      - "127.0.0.1:8080:8080/tcp"
      - target: 8081
        published: "8081"
        protocol: tcp
        host_ip: "${GENERALS_ADMIN_HOST:?set GENERALS_ADMIN_HOST}"
      - "443:8443/tcp"
      - "80:8083/tcp"
    volumes:
      - type: bind
        source: /srv/generals/data
        target: /data
        bind:
          create_host_path: false
      - type: bind
        source: /srv/generals/tls
        target: /tls
        read_only: true
        bind:
          create_host_path: false
      - type: bind
        source: /run/generals-server/admin-token
        target: /run/secrets/admin-token
        read_only: true
        bind:
          create_host_path: false
    tmpfs:
      - /tmp:size=16m,mode=1777,noexec,nosuid,nodev
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    logging:
      driver: awslogs
      options:
        awslogs-region: ${AWS_REGION:?set AWS_REGION}
        awslogs-group: ${CLOUDWATCH_LOG_GROUP:?set CLOUDWATCH_LOG_GROUP}
        awslogs-stream: generals-server
        mode: non-blocking
        max-buffer-size: 4m
